#
# AutoGenerated Makefile
# Generated by <%=$PROGRAM_NAME%> at <%=Time.new%>
#
#

VCS = vcs -sverilog -timescale=1ns/1ns +acc +vpi \
      $(UVM_HOME)/src/dpi/uvm_dpi.cc -CFLAGS -DVCS

SIMV = ./simv -l test.log

FILES =        \
<%@src_files.each do |f|%>
        <%=f%>  \
<%end%>

INCLUDES =    \
<%@src_incdirs.each do |d|%>
          +incdir+<%=d%> \
<%end%>

# +acc, +vpi is required for examples using backdoor access.

vcs:
	$(VCS) $(INCLUDES) $(FILES) main.sv  && $(SIMV)

